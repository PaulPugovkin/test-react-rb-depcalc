import { createAction } from '@reduxjs/toolkit';

export const userDepositProperties = createAction('deposit/addUserData');
export const userDepositType = createAction('deposit/addUsetDepositType');
export const modal = createAction('deposit/modal');
